import Foundation

enum AppPresentationDefaults {
    /// When `true`, use `.regular` (Dock + app switcher). When `false`, `.accessory` (menu bar only).
    static let showInDockAndAppSwitcherKey = "showAppInDockAndAppSwitcher"
}
